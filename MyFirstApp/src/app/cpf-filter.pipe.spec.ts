import { CpfFilterPipe } from './cpf-filter.pipe';

describe('CpfFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
