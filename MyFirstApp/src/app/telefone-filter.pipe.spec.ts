import { TelefoneFilterPipe } from './telefone-filter.pipe';

describe('TelefoneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
